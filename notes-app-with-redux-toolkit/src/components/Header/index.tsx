import React from "react";

interface Props {}

const Header = (props: Props) => {
  return (
    <h1 className="text-center text-4xl text-gray-500 font-bold">NotesApp</h1>
  );
};

export default Header;
